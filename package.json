{
  "dependencies": {
    "static-handler": "^0.4.3"
  },
  "name": "locus",
  "version": "0.0.1",
  "description": "A system for producing VR content for the web.",
  "scripts": {
    "asbuild:debug": "asc --config asconfig.json --target debug",
    "asbuild:release": "asc --config asconfig.json --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release",
    "start": "npx static-handler --cors --coop --coep --corp -p 8080 ."
  },
  "author": "supremestdoggo",
  "license": "MIT",
  "devDependencies": {
    "assemblyscript": "^0.27.22"
  }
}